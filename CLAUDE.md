# 项目约定

本文件是项目级约定，优先于默认行为。

## Git 工作流

- **提交与推送时机**：改动完成后**不要**自动 `git commit` / `git push`。只有用户明确说「提交」（或同等的明确指令）时才提交并推送。
- **分支**：本项目是单人开发，默认直接在 `main` 上开发并提交，不要为每次改动切分支或开 PR。只有用户明确要求「开分支 / 新建分支」时才创建分支。
- **提交信息格式**：`type(scope): 中文描述`（例如 `fix(player): 切歌清除残留调性`）。
- **暂存**：显式指定路径 `git add <具体文件>`，禁止 `git add -A`，避免误提交无关文件。
